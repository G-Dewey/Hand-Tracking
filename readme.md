# Hand-Tracking-Demo
has to be ran in python 3.10.14 or below as media pipe doesn't work for newer versions

required libaries
pip install opencv-python
pip install mediapipe==0.10.9
pip install pygame

to do hands have to be in frame to be detected

.\.venv\Scripts\python.exe main.py